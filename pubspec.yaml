name: myproject
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  convex_bottom_bar: ^3.0.0
  curved_navigation_bar: ^1.0.3
  image_picker: ^0.8.7+3 # สำหรับการเลือกและถ่ายภาพ
  cupertino_icons: ^1.0.8
  url_launcher: ^6.0.20
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # เพิ่ม assets ที่ต้องการใช้งาน (เช่นโฟลเดอร์ images)
  assets:
    - images/QR.png
    - images/logohorplus.png
